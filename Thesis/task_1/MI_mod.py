IO_set_1 = set("hello","drug",('media','file'))
IO_set_2 = set('hello','secret',('media','file'))
IO_set_ij = set('hello',frozenset({'data'}))
def Set_AI (*kwargs):
    for token in kwargs:
        if token = frozenset({"data from expert"}):
            result = "Alarm"
            else:
            continue
    return result
