
IO_set_1 = set("hello","drug",('media','file'))
IO_set_2 = set('hello','secret',('media','file'))
IO_set_ij = set('hello',frozenset({'data'}))
def MI_mod(*args):
    result = 'Alarm'
    token = 0
    for token in IO_set_1:
        if token = len(drug, secret or frozenset({"data"}):
            result = "Alarm"
        else:
            continue
    return result
    print(f'{mod_MI(IO_set_1=input("Введите множество))}')

